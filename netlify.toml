[build]
  base = "frontend"
  command = "CI=true node scripts/build-env.js && npm install --legacy-peer-deps && npm run build --legacy-peer-deps"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200